#ifndef MACRO_HELPER_HPP
#define MACRO_HELPER_HPP
#include <string>
#include <vector>
#include <sstream>
namespace Helper {
    std::vector<std::string> split(const std::string& input, char delimiter);
}

#endif